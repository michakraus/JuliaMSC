# Revision History

Changes to *JuliaMSC*. Each entry is a pass, scoped by chapter; the colour used for its
markings is recorded so that a marking in the text can be traced back to what prompted it.
Structural changes — chapters moved, split, merged — get their own section, because a per-file
diff does not show them.

This file was started on 2026-08-31 and holds no entries. Every pass made before that date is
in `git log` only, and the colour each one used is not recorded anywhere in the tree — a gap
this file cannot fill retroactively. Nothing has been reconstructed.
